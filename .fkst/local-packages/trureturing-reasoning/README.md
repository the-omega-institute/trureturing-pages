# trureturing-reasoning

Phase C/D will connect this package to department dispatch, produce, verify,
consensus, and record flows. The Phase B state-machine implementation lives in
the repository `lib/` package.
